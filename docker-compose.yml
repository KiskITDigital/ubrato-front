services:
  front:
    image: ubrato-front
    container_name: ubrato-front
    build:
      context: .
      args:
        VITE_SERVER_URI: https://api.ubrato.ru
        VITE_CDN_URI: https://cdn.ubrato.ru
        VITE_TYPESENSE_API_KEY: NXrcTzQffx7t0N6t0QKtlQ3Enbh7Q5DR
    restart: unless-stopped
    networks:
      - ubrato-front-network
    ports:
      - "8003:80"

networks:
  ubrato-front-network:
    name: ubrato-front-network
